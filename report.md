# 最终项目报告：用身体与选秀信息预测 NBA 场均得分

> 本报告记录可复查证据。所有数字来自 `python main.py` 与 `python -m unittest discover -s tests -v` 的真实输出。

## 摘要

球队数据分析师需要在考察球员前快速得到一份"得分期望"。本项目用 Kaggle 真实数据集（1996–2022 年 NBA 球员赛季记录，12844 行）训练一个回归模型：以身高、体重、年龄、选秀轮次与顺位为输入，预测球员单赛季场均得分。基线为全体平均分（MAE 4.877 分/场）；候选为随机森林回归（MAE 3.053 分/场，降低 37.4%）。模型在测试集上显著优于基线，但会严重高估/低估个别球员（如伤愈赛季的 Gordon Hayward 被高估 19.5 分），说明静态信息无法解释伤病、体系与技术因素，因此只适合作为初步筛选，不能替代人工判断。

## 1. 问题与范围

- 项目句子：我们要帮助【球队数据分析师】，根据【球员的身高、体重、年龄、选秀轮次与顺位】，预测【该球员某赛季的场均得分】，以便【在考察/签约前快速得到一份粗略的得分期望】；我们尤其不能忽略【身体与选秀信息解释不了的技术流或伤病型球员，模型给出看似精确实则误导的数字】。
- 使用者与小决定：球队数据分析师决定"哪些球员值得进一步深挖"。
- 输入与输出：输入 = age、player_height、player_weight、draft_round、draft_number（均为进入联盟前可知）；输出 = 场均得分 pts（连续数值，分/场）。
- 普通案例：输入身高 201cm、体重 104kg、年龄 22、次轮第 30 顺位 → 输出一个大致得分期望（例如 12 分左右量级）。
- 困难/拒绝案例：输入一位技术流后卫（身高 185cm）→ 模型预测偏低；系统在失败案例中把该类球员标出，提示"依据不足，需人工复核"。
- 核心范围：只做单赛季场均得分的回归估计，不预测未来生涯、伤病或团队适配。
- 明确不做：不做"预测新秀首赛季"（需要配对数据且样本极小）、不做分类、不做部署或界面。

## 2. 真实数据契约

- 所有者/发布者：Justinas Cirtautas（Kaggle 用户 justinas）
- 数据集标题与版本：NBA Players（all_seasons.csv，含 1996–2022 赛季）
- 原始 URL：https://www.kaggle.com/datasets/justinas/nba-players-data
- 许可、同意或使用权限：Kaggle 页面标注 "Other (specified in description)"；已打开描述页核对发布者说明，需向教师确认后以截图存档
- 下载日期与步骤：2026-08-25；`pip install kagglehub` 后 `kagglehub.dataset_download('justinas/nba-players-data')`，将 `all_seasons.csv` 复制到 `data/raw/`
- 预期文件、字段、数量范围：`all_seasons.csv`，实际 12844 行、22 列；关键字段 player_name、age、player_height、player_weight、draft_round、draft_number、pts
- 数据检查命令与结果：`python main.py` 首步打印 `行数: 12844`、列名清单，`数据契约检查通过`；单测 `python -m unittest discover -s tests -v` 全部通过
- Git 排除：`data/raw/`、`.env`、`.venv/`、`__pycache__/`、`*.pyc`、`models/`、`checkpoints/` 由 `.gitignore` 排除
- 敏感字段：无个人隐私或密钥；不包含医疗/法律/安全决策内容
- 缺失、偏差、代表性与时间限制：该版本无 position 列；落选球员的 draft_round/draft_number 为文本 "Undrafted"（共 4825 个，已编码为 0）；身高体重单位需在下载后核对；数据截止 2022 赛季
- 允许/不允许的结论：允许"候选显著优于基线（37.4%）"；不允许"模型能判断球员真实水平"或"可用于替代球探决定"

## 3. 系统设计

```
真实输入(all_seasons.csv) → 数据检查 → 数据清理(Undrafted→0) → 特征准备(z-score) → 固定划分(seed=42, 80/20) → 基线(平均分) → 候选(随机森林) → 比较MAE → 失败案例 → results/summary.txt
```

每个组件均为最小必要：数据检查验证"拿到预期格式"；清理把非数值选秀值编码为 0（否则模型无法训练）；z-score 让数值特征尺度一致；固定划分保证基线与候选同条件比较；MAE 直接对应"平均差多少分"的使用者语义。

## 4. 可复现方法

- 环境和依赖：Python 3.14 + scikit-learn（见 `requirements.txt`）
- 安装命令：`python -m pip install -r requirements.txt`
- 数据放置：`data/raw/all_seasons.csv`
- 运行命令：`python main.py`
- 测试命令：`python -m unittest discover -s tests -v`
- 输出文件：`results/summary.txt`（基线/候选 MAE 与改善）
- 随机种子、时间范围或固定条件：随机种子 42，随机 80/20 划分；未按时间切分（特征均为选秀前可知的静态信息，无未来信息进入输入）

## 5. 基线与候选

- 基线：全体训练球员的平均场均得分（8.17 分/场）。最简单、可解释、几秒可复现，也是"无信息预测"的下限。
- 候选：随机森林回归（100 棵树），输入身高/体重/年龄/选秀轮次/顺位。选择理由：实验显示线性模型几乎不优于基线（+0.004），而树模型能捕捉特征间的非线性，且可输出特征重要性用于解释。
- 只复杂一步：从"一个常数"到"一个标准回归模型"，未堆叠多个模型或调参搜索；固定种子与划分，与基线完全同条件比较。

## 6. 评估设计与结果

- 划分/评估协议：随机种子 42，80/20 划分，同一测试集（2568 样本）上比较；训练 10276 样本。
- 主指标与使用者含义：平均绝对误差 MAE（分/场），直接回答"预测平均差几分"。
- 辅助错误计数：失败案例数量（误差最大的具体球员）；数据清理量（4825 个落选值）。
- 测试集如何保持独立：划分在特征构建后随机固定，基线与候选使用完全相同的数据。

| 结果 | 基线 | 候选 | 条件与解释 |
| --- | ---: | ---: | --- |
| 主指标 MAE（分/场） | 4.877 | 3.053 | 同一测试集 2568 样本，降低 37.4% |
| 重要错误 | — | 高估/低估技术流与伤病球员 | Hayward 被高估 19.5 分 |
| 特征重要性（top） | — | draft_number 0.348 | 顺位是最强信号，其次体重 0.251、年龄 0.192、身高 0.161、轮次 0.048 |

## 7. 成功与失败案例

**成功案例（模型预测接近真实）**：测试集中大量普通球员的预测落在 ±3 分内，MAE 3.053 意味着平均每个预测与真实得分相差约 3 分，相比基线平均差 4.9 分有实质改进。证据：`python main.py` 输出的 MAE 对比与 `results/summary.txt`。

**失败案例（误差最大）**：
- Gordon Hayward：真实 2.0 / 预测 21.5（误差 19.5）。2019–20 赛季 Hayward 因伤仅场均 2.0 分，而模型依据其历史级别的身体与高顺位给出 21.5 的高估。
- Amar'e Stoudemire：真实 25.3 / 预测 7.2（误差 18.1）。模型大幅低估这位技术流内线。

这说明：模型基于的是静态档案，**无法感知伤病、复出时间、体系与技术风格**。失败案例证明"预测不等于球员真实水平"，但不能证明模型在普通球员上也失效——MAE 显示整体仍显著优于基线。

## 8. 安全、伦理与人工边界

- 无密钥、无个人隐私、无敏感数据；数据集许可需教师确认。
- 不涉及医疗/法律/安全等高风险自动决策；输出仅供初步筛选。
- 人工边界：模型只给"得分期望"这一单一数字，不评价防守、球商、伤病风险与团队适配；对误差最大的球员，报告要求人工复核，不把预测当精确值。

## 9. 智能体贡献与学生所有权

- 智能体建议/生成：帮助定位数据源与字段核对；生成了 `main.py` 骨架（数据检查、清理、划分、随机森林候选、结果保存）与 `tests/`、`make_ppt.py`；报告与 PPT 初稿。
- 学生的核对与修改：核对数据列名（该版本无 position 列）、确认落选值 "Undrafted" 并决定编码为 0、复跑实验比较线性回归/决策树/随机森林、核对所有报告数字与 `python main.py` 输出一致。
- 拒绝的建议：拒绝加入 country 特征与随机调参（保持范围最小、可解释）；拒绝把失败案例替换为更"好看"的样本。
- 每名成员能独立解释：数据契约检查、Undrafted 清理、固定划分、随机森林与 MAE 的含义、失败案例 Hayward 说明的边界。

## 10. 限制与下一步

- 数据限制：该数据集无 position 列；静态信息不含大学表现；截止 2022 赛季。
- 方法限制：随机森林对"极端个体"（伤病、技术流）外推能力弱；未做超参数搜索（有意为之）。
- 评估限制：随机 80/20 而非按球员分组，同一球员多个赛季可能同时出现在训练与测试；未按时间切分。
- 产品限制：只输出单一得分估计，不做任何决策建议。
- 下一步最小改进：改用**按球员分组**的划分消除跨集泄漏，并加入 `country` 特征（实验显示随机森林 MAE 可再降至 2.91），用同一测试集重新比较以验证是否真实改善。

## 11. 最终复核

- [x] 新环境可按 README 运行
- [x] 数据契约与检查完整
- [x] 基线和候选同条件
- [x] 测试/评估与保存输出一致
- [x] 失败案例和边界具体
- [ ] 10 分钟 PPT 计时通过
- [ ] 每名成员能回答教师追问
- [x] 无密钥、大数据、个人信息、环境或缓存
- [ ] GitHub 网页和访问权限复查
